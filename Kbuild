obj-m := mydriver.o
mydriver-objs += mydriver-dev.o

subdir-y := device-trees
#obj-y := device-trees/

#dtb-y := device-trees/overlays/raspberrypi-5b-csi2-mydriver.dtbo
#targets += $(dtb-y)
#always  := $(dtb-y)
